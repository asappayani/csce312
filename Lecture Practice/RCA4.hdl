CHIP RCA4 {
    IN a[4], b[4], c;
    OUT sum[4], cout;

    PARTS:
    
    FullAdder(a=a[0], b=b[0], c=false, sum=sum[0], carry=c0out);
    FullAdder(a=a[1], b=b[1], c=c0out, sum=sum[1], carry=c1out);
    FullAdder(a=a[2], b=b[2], c=c1out, sum=sum[2], carry=c2out);
    FullAdder(a=a[3], b=b[3], c=c2out, sum=sum[3], carry=cout);
}